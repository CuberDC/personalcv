import "./GithubProfileCard.scss";

export default function GithubProfileCard({prof}) {
  return (
    <div style={{display: 'none'}}>
      {/* GitHub Profile Card disabled to avoid duplicate contact sections */}
    </div>
  );
}
